steps:
  - id: build
    name: gcr.io/k8s-skaffold/pack
    args: ["build", "$_IMAGE_NAME", "--builder=gcr.io/buildpacks/builder"]
  - id: push
    name: gcr.io/cloud-builders/docker
    args: ["push", "$_IMAGE_NAME"]
  - id: migrate
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: /bin/bash
    args:
      - "-c"
      - |
        gcloud beta run jobs deploy migrate \
          --region $_REGION \
          --image "$_IMAGE_NAME" \
          --service-account 724115144699-compute@developer.gserviceaccount.com \
          --set-env-vars SETTINGS_NAME=$_SECRET_SETTINGS_NAME \
          --command migrate \
          --execute-now
options:
  dynamicSubstitutions: true
substitutions:
  _REGION: us-central1
  _SECRET_SETTINGS_NAME: django_settings
  _IMAGE_NAME: ${_REGION}-docker.pkg.dev/rocket-request/cloud-run-source-deploy/distribute-service
images:
  - "$_IMAGE_NAME"
